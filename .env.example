# PostgreSQL (required for Vercel). Free tier: https://neon.tech
# Use the "connection string" with ?sslmode=require
DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/neondb?sslmode=require"

# openssl rand -base64 32
NEXTAUTH_SECRET=generate-a-long-random-string

# Local: http://localhost:3000
# Vercel: https://your-app.vercel.app (must match the deployed URL)
NEXTAUTH_URL=http://localhost:3000

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=+1...
TWILIO_VERIFY_SID=VA...

RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=noreply@swiftship.in

RAZORPAY_KEY_ID=rzp_test_...
RAZORPAY_KEY_SECRET=
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_...

# Must match the browser origin (local or Vercel URL)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_COMPANY_NAME=SwiftShip

# --- Vercel quick demo ---
# 1. Create a Neon database; set DATABASE_URL in Vercel Project → Settings → Environment Variables (Production + Preview).
# 2. Set NEXTAUTH_SECRET, NEXTAUTH_URL, NEXT_PUBLIC_APP_URL to your https://*.vercel.app URL.
# 3. Deploy (Git push or Vercel CLI). Build runs: prisma migrate deploy && next build
# 4. One-time seed (from your machine): DATABASE_URL="your-neon-url" npx prisma db seed
#    Login: test@swiftship.in / Test@1234 — track AWBs SW2025011401 … SW2025011405
